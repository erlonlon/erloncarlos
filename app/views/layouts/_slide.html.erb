 <div id="featured_slide">
    <div id="slider" class="nivoSlider">
    		<% @galleries_eventos.galleries.limit(6).each do |g| %>
          	 	 <%= image_tag(g.foto.url(:small)) %>
            
              <% end %>
          </div>
  </div>



 